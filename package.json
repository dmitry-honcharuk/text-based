{
  "name": "find-stuff",
  "version": "1.0.0",
  "main": "dist/index.js",
  "license": "MIT",
  "scripts": {
    "test": "jest",
    "clear": "rimraf dist/",
    "dev": "npm run build && concurrently \"tsc --watch\" \"nodemon\"",
    "build": "npm run clear && tsc"
  },
  "devDependencies": {
    "@types/faker": "^4.1.12",
    "@types/jest": "^26.0.12",
    "jest": "^26.4.2",
    "nodemon": "^2.0.4",
    "rimraf": "^3.0.2",
    "ts-jest": "^26.3.0",
    "typescript": "^4.0.2"
  },
  "dependencies": {
    "faker": "^5.1.0"
  }
}
