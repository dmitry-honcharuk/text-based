{
  "name": "find-stuff",
  "version": "1.0.0",
  "main": "dist/index.js",
  "license": "MIT",
  "scripts": {
    "test": "jest",
    "test:coverage": "yarn test --coverage",
    "test:ci:coverage": "CI=true yarn test:coverage",
    "clear": "rimraf dist/",
    "dev": "npm run build && concurrently \"tsc --watch\" \"nodemon\"",
    "build": "npm run clear && tsc"
  },
  "devDependencies": {
    "@types/faker": "^4.1.12",
    "@types/jest": "^26.0.12",
    "concurrently": "^5.3.0",
    "jest": "^26.4.2",
    "nodemon": "^2.0.4",
    "rimraf": "^3.0.2",
    "ts-jest": "^26.3.0",
    "typescript": "^4.0.2"
  },
  "dependencies": {
    "@types/cors": "^2.8.7",
    "@types/express": "^4.17.8",
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "express-validator": "^6.6.1",
    "faker": "^5.1.0",
    "joi": "^17.2.1"
  }
}
